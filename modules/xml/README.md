<!-- SPDX-License-Identifier: MIT -->

# SharpRuntime::Xml

Compiled XML and XPath module. Direct dependencies: `Core` and `Diagnostics`.
External dependency: vendored tinyxml2. `SharpRuntime::Xml.XPath` aliases this
same physical archive because XML and XPath have mutual binary dependencies.
