# Audit: `modules/net/include/System/Net/EndPoint.hpp`

Audit status: AUDITED.

Base methods deliberately throw the project `NotImplementedException`, matching
the managed base-class pattern.  No separate finding was confirmed.
