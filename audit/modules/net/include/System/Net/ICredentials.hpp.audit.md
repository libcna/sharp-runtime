# Audit: `modules/net/include/System/Net/ICredentials.hpp`

Audit status: AUDITED.

The interface correctly represents nullable managed credential lookup with a
`shared_ptr` result.  No separate finding was confirmed.
